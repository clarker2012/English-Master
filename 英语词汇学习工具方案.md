# 英语词汇学习工具方案分析与 Codex 开发输入

## 一、可行性分析

下面这个方案方向是对的，但原始目标需要调整，否则工具会变成“功能很多但学习效果不稳定”的产品。

### 1. “1–2个月从4000–6000提升到20000+”不现实

如果当前词汇量约 4000–6000，要到 20000，意味着新增约 14000–16000 个词。60 天内完成，相当于每天新增 230–270 个词，而且还要能发音、理解中文意思、会在语境中使用。这个目标更接近“短期大量眼熟”，不等于真正掌握。

更合理的目标是：

**1–2个月：把高频词、托福/雅思核心词、学术词汇提升到 7000–10000 左右。**  
**6–12个月：冲击 12000–16000。**  
**1–2年：逐步接近 20000+。**

所以，工具目标应该改成：

> **短期目标：快速提升听力和阅读可用词汇量。**  
> **长期目标：通过分级短文 + 朗读 + 间隔复习，把词汇量逐步扩展到 20000 级别。**

---

### 2. 词汇量测试可以做，但不能追求“绝对精准”

工具可以做词汇量估算，但最好不要说“精准测出”。更准确的说法是：

> 通过抽样测试估算词汇量范围，例如 4000–5000、5000–6000、6000–8000。

词汇测试会受到猜题、被动认识、词族推断等因素影响，因此结果应显示为区间，而不是一个绝对数字。

---

### 3. “短文记词”是可行的，而且比孤立背词更适合听力

这个方向很好。单词放进句子、短文、朗读、复述，比单独记忆更接近真实语言使用。更推荐的机制是：

**单词输入 → 句子 → 微型短文 → 听读 → 跟读 → 回忆测试 → 间隔复习。**

但有一个关键问题：  
系统不能每次只把 100 个新词硬塞进一篇文章，否则短文会非常不自然。应该使用：

**已掌握词 70–90% + 新词 10–30% + 复习词若干。**

原方案中 “新词比例从 10% 到 50%” 可以保留，但 50% 只适合极短文本或复习测试，不适合正常学习文本。正常阅读/听力材料里，新词过多会导致理解崩溃。

---

### 4. “连续出现100次不点击就算掌握”这个指标过于机械

这个指标有价值，但应该改成综合评分。因为一个词出现 100 次没有点击，不代表真的会用、会读、会听懂。

建议把掌握度分成 0–5 级：

| 等级 | 状态 | 判断依据 |
|---|---|---|
| 0 | 陌生 | 点击查看、测试错误 |
| 1 | 见过 | 能认出中文意思 |
| 2 | 初步掌握 | 能听懂/读懂 |
| 3 | 基本掌握 | 能朗读正确，短测正确 |
| 4 | 熟练 | 多次不点击，听读都正确 |
| 5 | 长期掌握 | 多天后复测仍正确 |

“连续 100 次出现不点击”可以作为升到 5 级的条件之一，但不要作为唯一条件。

---

### 5. 语音领读功能可做，但发音评分有技术限制

HTML 网页可以用浏览器的 **Web Speech API** 做文本朗读，也可以用 SpeechRecognition 做语音识别。

但是，浏览器自带语音识别对发音标准度的评分能力有限。它能判断用户读出来的文本是否接近原文，但不能像专业口语考试系统那样精准评分音素、重音、连读、弱读。

所以可行方案是：

**MVP 版本：**
用浏览器 TTS 领读，用麦克风录音或 SpeechRecognition 检查用户读得是否接近原句。

**高级版本：**
接入 Azure Speech、Google Speech、OpenAI Whisper 或类似服务，做更准确的发音、流利度、语速评分。

---

## 二、建议修改后的产品方案

这个工具应该设计成：

> **一个基于 HTML/CSS/JavaScript 的英语词汇学习 SPA 工具，通过词汇量测试、分级词库、AI 生成短文、语音领读、跟读评分、点击释义、间隔复习，帮助学习者从 4000–6000 词逐步提升到 10000、15000、20000 词。**

核心模块：

1. **词汇量测试模块**  
   分频率层级抽样测试，估算词汇量范围。

2. **词库模块**  
   20000 个词分成 200 组，每组 100 个。每个词包含：  
   word, phonetic, Chinese meaning, part of speech, frequency rank, example sentence, difficulty level。

3. **短文生成模块**  
   每次根据学习者掌握度生成 50–200 词短文。  
   初期是短句，中期是多句，后期是完整短文。

4. **学习界面模块**  
   支持中英文对照开关、点击单词显示音标和中文意思、播放整句/整篇、单词高亮。

5. **语音领读与跟读模块**  
   120–200 WPM 动态调速。  
   每句播放后暂停 1.5 倍时间，用户跟读。  
   记录语速、识别文本相似度、是否流畅。

6. **掌握度与复习模块**  
   使用 SM-2 或类似间隔重复算法。  
   点击越多、错误越多，复习频率越高。  
   多次正确、不点击、跟读顺畅，则降低出现频率。

7. **数据存储模块**  
   使用 localStorage 或 IndexedDB 保存学习记录。  
   支持导入/导出 JSON。

---

## 三、给 Codex 的开发输入

下面这段可以直接复制给 Codex。

```text
你是一个资深前端工程师和英语学习产品设计师。请帮我开发一个英语词汇学习网页工具，使用 HTML + CSS + JavaScript 实现，优先做成一个可以直接打开的单页应用。第一版可以使用本地 mock 数据，但代码结构要支持未来导入 20000 个英语词汇 JSON 数据。

产品名称：Context Vocabulary Trainer

目标用户：
中文母语的英语学习者，当前英语词汇量约 4000–6000，目标是通过短文、朗读、复习和测试，逐步提升到 10000、15000、20000 级别。

核心目标：
1. 估算学习者当前英语词汇量范围。
2. 把 20000 个常用词按每 100 个词分成一组，共 200 个学习单元。
3. 根据学习者掌握情况，把陌生词、复习词和已掌握词混合成句子或短文。
4. 使用短文学习单词的发音、中文意思、使用方法。
5. 支持中英文对照开关。
6. 支持点击单词查看音标、词性、中文意思、例句。
7. 支持美式英语语音领读。
8. 支持用户跟读，并记录语速、朗读次数、是否点击释义、是否答题正确。
9. 根据学习记录动态调整新词比例和复习词比例。
10. 保存学习进度，支持导入导出数据。

技术要求：
- 使用原生 HTML/CSS/JavaScript。
- 不依赖后端。
- 使用 localStorage 或 IndexedDB 保存学习数据。
- 使用 Web Speech API 的 speechSynthesis 做美式英语领读。
- 如果浏览器支持 SpeechRecognition，则加入跟读识别功能；如果不支持，则显示提示并允许用户手动标记“朗读顺畅/不顺畅”。
- 页面要美观、现代、响应式，适配桌面和手机。
- 代码要模块化，易于扩展。
- 第一版请生成 index.html 一个文件即可，内含 CSS 和 JS。
- 代码里放入 30–50 个 mock 单词用于演示，未来可以替换成 20000 词 JSON。

数据结构设计：
每个单词对象包含：
{
  id: number,
  word: string,
  phonetic: string,
  pos: string,
  zh: string,
  level: number,
  frequencyRank: number,
  groupId: number,
  example: string,
  knownScore: number,
  clickCount: number,
  correctCount: number,
  wrongCount: number,
  readCount: number,
  lastReviewedAt: string,
  nextReviewAt: string,
  status: "new" | "learning" | "review" | "mastered"
}

用户学习状态包含：
{
  estimatedVocabulary: number,
  currentGroupId: number,
  masteredWords: number,
  learningWords: number,
  newWordRatio: number,
  targetWpm: number,
  totalStudyMinutes: number,
  streakDays: number
}

页面结构：
1. 顶部 Dashboard
   - 显示估算词汇量
   - 已掌握词数
   - 今日学习词数
   - 当前语速 WPM
   - 新词比例

2. 词汇量测试页面
   - 从不同词频等级中抽样出题
   - 题型：选择中文意思、选择英文释义、是否认识该词
   - 测试结束后给出区间估算，例如 4000–5000、5000–6000、6000–8000
   - 根据结果设置初始学习等级

3. 学习页面
   - 显示当前短文标题
   - 显示英文短文
   - 每个单词可点击
   - 点击后显示浮层：音标、中文意思、词性、例句
   - 中文翻译有开关，可以显示或隐藏
   - 生词用下划线或高亮显示
   - 复习词用另一种样式显示

4. 领读功能
   - 按钮：播放整篇
   - 按钮：播放当前句
   - 按钮：逐句领读
   - 语速范围 120–200 WPM
   - 默认根据用户熟练度设置：
     低熟练度：120–140 WPM
     中熟练度：140–170 WPM
     高熟练度：170–200 WPM
   - 每播放一句后，暂停该句播放时间的 1.5 倍，提示用户跟读

5. 跟读评估
   - 如果支持 SpeechRecognition：
     启动麦克风识别用户朗读内容
     比较识别文本和原句的相似度
     计算 readingAccuracy
     估算用户 WPM
   - 如果不支持：
     提供按钮：
       “我读得顺畅”
       “我读得一般”
       “我读得困难”
   - 根据结果调整 targetWpm 和单词掌握度

6. 复习机制
   - 每个词有 knownScore，范围 0–5
   - 点击查看释义：knownScore 减少或不增加
   - 测试答对：knownScore 增加
   - 朗读顺畅：knownScore 增加
   - 多次复习仍正确：进入 mastered
   - mastered 词降低出现频率，但仍偶尔出现在短文中
   - learning/review 词高频进入短文

7. 动态新词比例
   - 初始 newWordRatio = 0.1
   - 如果用户连续多次学习正确率高于 85%，并且跟读顺畅，则逐步增加到 0.2、0.3、0.4
   - 最高不超过 0.5
   - 如果正确率低于 70%，降低新词比例
   - 正常学习文本建议 10%–30% 新词，50% 仅用于挑战模式

8. 短文生成逻辑
   - 根据当前学习单元选择新词
   - 根据 nextReviewAt 选择需要复习的词
   - 混合部分已掌握词，让短文自然
   - 初学者短文长度 20–50 words
   - 中级 50–120 words
   - 高级 120–200 words
   - 最长不超过 200 words
   - 第一版可以用模板生成短文，不需要调用 AI
   - 例如根据目标词自动生成 3–8 个句子

9. 统计页面
   - 显示学习天数
   - 总朗读次数
   - 已掌握词数
   - 待复习词数
   - 估算词汇量变化
   - 最近 7 天学习曲线
   - 各 group 完成度

10. 导入导出
   - 导出 progress.json
   - 导入 progress.json
   - 未来支持导入 vocabulary.json

掌握度算法建议：
- knownScore 初始为 0
- 用户点击释义：clickCount +1，不增加 knownScore
- 选择题答对：correctCount +1，knownScore +0.5
- 选择题答错：wrongCount +1，knownScore -0.5
- 朗读顺畅：readCount +1，knownScore +0.3
- 连续多次不点击且答对：knownScore 增加
- knownScore >= 4.5 且多天复习正确，则 status = mastered
- mastered 词仍按低频率复现
- 如果 mastered 词后续答错，则退回 review

语音部分实现要求：
- 使用 window.speechSynthesis
- 选择 en-US voice
- rate 根据 targetWpm 映射到 speechSynthesisUtterance.rate
- 播放时当前句高亮
- 播放完成后进入跟读倒计时
- SpeechRecognition 要兼容 webkitSpeechRecognition
- 如果不可用，给出 graceful fallback

UI 风格：
- 干净、现代、类似学习 App
- 卡片式布局
- 顶部显示学习数据
- 中间是短文阅读区
- 右侧或底部是单词详情和控制按钮
- 生词、复习词、已掌握词用不同视觉标记
- 手机端自动变成单列布局

请输出完整 index.html 代码。
代码要包含：
1. HTML 结构
2. CSS 样式
3. JS 状态管理
4. mock vocabulary 数据
5. 词汇量测试逻辑
6. 短文生成逻辑
7. 点击单词显示释义
8. 中文显示开关
9. TTS 领读
10. 跟读识别或手动反馈
11. localStorage 保存学习进度
12. 统计面板
13. 导入导出功能

不要只写伪代码，要给出可以直接运行的完整代码。
```

---

## 四、建议开发路线

第一版重点做这些：

1. 词汇量测试  
2. 短文学习  
3. 点击查词  
4. 中文开关  
5. 美式领读  
6. 手动/自动跟读反馈  
7. 掌握度记录  
8. localStorage 保存进度  

不要一开始就强行实现完整 20000 词库、专业发音评分、AI 短文生成。那样开发量会膨胀。更好的路线是：

**MVP 先跑通学习闭环，再扩充词库和 AI 生成能力。**
